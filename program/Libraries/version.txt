V1.0
    增加硬件IIC卡死检测，避免硬件IIC卡死
    修改串口 IIC SPI引脚复用设置错误问题
    
V1.1
    修正注释错误
    GPIO增加单独的上下拉电阻设置函数，便于切换
    修改OLED数字显示函数多余操作，提高速度
    OLED增加oled_printf_int32 oled_printf_float函数可以去除无效0位
    修改OLED画点函数，使得可以控制OLED的每一个像素，从而可以使用OLED画出任意图形
    
V1.11
    修复特殊情况不能复位的问题
    将inc src源代码设置为不加锁，便于修改代码
    增加永久关闭ISP选项
    摄像头驱动程序增加中断优先级设置
    
V1.12
    修复TFT lcd_showint32与lcd_showfloat函数变长度显示时会保留上一次的数据的问题
    
V1.13
    修复OLED oled_printf_int32与oled_printf_float函数变长度显示时会保留上一次的数据的问题
	
V1.14
	修复GINT组中断  默认为与连接的问题  将默认设置改为或链接
    去掉GINT初始化函数无用参数，增加单独的引脚中断启用与禁用函数
    修复硬件IIC等待延时逻辑错误
    修改硬件IIC默认的延时等待时间为10000
	修复SPI片选参数不使用引脚的时，进入断言失败
	修复SPI的片选引脚计算错误的问题
    SPI初始化函数增加模式设置 spi_mosi函数增加是否发送EOT的参数
    增加ICM20602驱动程序   驱动程序包含软件IIC 硬件IIC 硬件SPI三种通信方式
    修改1.8寸TFT 硬件spi波特率为20M，提高液晶的显示速度。
	修改PIT初始化函数默认使能PIT中断
	修改引脚功能说明文件 错误信息
    增加MRT定时器，使得PIT周期中断可以增加4路，4路PIT使用的是同一个中断，在中断里使用标志位区分
	修改eeprom字写入函数的数据形参为uint32 *指针，填写参数时需要变更为需要保存的变量的地址
	修改eeprom读取宏定义函数，增加type参数，用于设置读取的数据类型
	修复eeprom最大偏差宏定义错误问题
    修复flexcomm8不能使用的问题
	软件IIC引脚初始化默认加上OD 开漏
    修改频率设置LPC546XX_MAIN_CLOCK为2时，频率设置为210M
    将摄像头场中断修改为使用PINT引脚中断触发
    增加SCT输出PWM的功能，由于摄像头的PCLK和PWM周期需要各占用一个通道，因此SCT一共十个通道最多只能输出8路PWM，可以用于控制4个电机正反转
    在常用的中断相关的初始化最后，添加开启中断及中断优先级设置函数的注释
    增加PIT计时清除函数
	
V1.15
	修改串口四引脚错误问题
    修改MMA8451的自检函数
    
V1.16
    修改IIC枚举定义错误的问题
    
V1.17
    DMA增加链接传输方式，可以实现任意长度的传输，从而摄像头可以实现采集完成才进入中断，大大减少dma中断次数。
    
V1.18
    更正摄像头的采集方案 并对DMA底层驱动进行一定修改
    
V1.19
    修复TIMER2_COUNT2_A10引脚复用设置错误
    修复当RAM使用较大时出现无法启动的问题
	修复SPI1 B10引脚复用设置错误问题
	修复当SPI1速率较高时，CS引脚出现延迟的问题
	修改小钻风的DMA中断关于采集完成的判断逻辑
    修复由于摄像头采集方式变化导致小钻风采集出错的问题
    修复SCT PWM函数关于断言错误的问题
	将模拟IIC引脚SCL SDA定义进行了调换，使其与硬件IIC对应
	删除A23 AD引脚需要断开电源检测的提示
	修改oled设置坐标函数是从像素的第二个点显示的问题
	
V1.20
	将1.19版本中的 模拟IIC引脚调换的操作进行撤销。这里说明下原因，是因为改了之后与7725默认接口就不一致了，所以改回来
	IO复用函数中限制SWD使用的IO（A11、A12）进行复用设置，避免对着两个引脚进行设置
	
V1.21
	修改ctimer中的 计数引脚枚举中的错误
    
V1.22
    修复硬件IIC引脚  切换为GPIO模式枚举值定义错误的问题。
    修改gpio初始化函数，对A13 A14单独设置配置
    修改CTIMER 以及 IIC引脚复用设置函数  对A13 A14两个引脚进行特别处理
	修改CTIMER 占空比设置函数中的断言设置
	将LPC的配置文件，改为仅在需要使用到的地方包含，当修改配置文件之后不用重复编译其他文件
    CTIMER定时器0 计数功能2通道引脚定义错误，将A28引脚修改为B28引脚
    对systick_timing函数进行声明，解决使用systick_timing_ms宏函数出现警告的问题
    添加IPS液晶驱动程序
    对common.h头文件包含位置进行更改，避免MDK在h文件出现红色的下划线
	
V1.23
	修复在IAR编译器下 sprintf 浮点BUG，导致各类显示屏显示浮点出现问题
	修改sprintf函数名为zf_sprintf,避免MDK去链接Keil C库下的sprintf
    这里提示 printf MDK不会自动链接到Keil C库下的printf，所以不用担心
    
V1.24
    修复由于同一个串口 、IIC、SPI重复初始化导致进入断言错误问题
    解决ADC模块可能异常，然后造成程序卡在while的问题

V1.25
	修复systick计时功能  使用systick计时先调用 systick_start然后在调用h文件对应的宏函数（如systick_getval_ms）
	修复pint中断 电平触发模式不能使用的问题  这里需要说明电平触发模式不能持续触发中断，比如设置为低电平触发，如果引脚持续为低电平中断不会一直响应
	如果想实现持续响应，可以在进入中断后，使用gpio_get函数检测IO状态，如果IO为低电平则不对中断标志位清除，则可以实现
	修复A27引脚无法输出PWM的问题
	1.25版本开始，需要使用IAR8.3版本才能打开库
	
	